import React from 'react';

const FormInput = (props) => {
    return (
        <div>
            <input 
                name={props.name} 
            />
        </div>
    );
};

export default FormInput;